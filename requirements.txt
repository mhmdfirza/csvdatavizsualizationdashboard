# Core dependencies
streamlit>=1.33.0
pandas>=2.2.0
numpy>=1.26.0

# Visualization
plotly>=5.20.0

# Export and image handling (chart PNG export)
kaleido>=0.2.1

# Faster CSV parsing for large files (with automatic fallback)
pyarrow>=15.0.0

# Optional: improves Streamlit's local dev auto-reload
watchdog>=4.0.0